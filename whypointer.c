#include <stdio.h> 

int main(void) {
    int numbers[10] = {1,2,3,4,5,6,7,8,9,0};
    printf("%d\n", numbers); //We will get the memmory address where contents of the array is stored :)
    return 0;
}